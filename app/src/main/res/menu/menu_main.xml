<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/view_button"
        android:icon="@drawable/view_list"
        android:title="@string/view_button"
        android:showAsAction="ifRoom"
        />
    <item android:id="@+id/sort_button"
        android:icon="@drawable/sort_books"
        android:title="@string/sort_button"
        android:showAsAction="ifRoom"
        />

    <item android:id="@+id/folder_chooser_button"
        android:title="Change Default Directory"
        />

    <item android:id="@+id/refresh_button"
        android:title="Refresh"
    />

    <item android:id="@+id/settings_button"
        android:title="@string/settings"
        />



</menu>
